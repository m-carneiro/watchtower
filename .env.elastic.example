# Elastic Cloud Configuration
# Get credentials from: https://cloud.elastic.co

# Option 1: Using Cloud ID + API Key (Recommended)
# Cloud ID format: deployment-name:base64-encoded-data
ELASTIC_CLOUD_ID=
ELASTIC_API_KEY=

# Option 2: Using Cloud ID + Username/Password
# ELASTIC_CLOUD_ID=
# ELASTIC_USERNAME=elastic
# ELASTIC_PASSWORD=

# Option 3: Using Direct Endpoint (for self-hosted)
# ELASTIC_ENDPOINT=https://your-deployment.es.region.cloud.es.io:443
# ELASTIC_API_KEY=  # or ELASTIC_USERNAME/ELASTIC_PASSWORD

# Watchtower Configuration
WATCHTOWER_API_URL=http://localhost:8080
WATCHTOWER_API_TOKEN=  # Optional: Leave empty if auth is disabled

# Feed Configuration
# Format: cef or stix
# CEF (Common Event Format) - Traditional log format
# STIX (Structured Threat Information) - Modern threat intelligence format (Recommended)
FEED_FORMAT=stix

# Fetch Interval: How often to run ingestion (for cron/systemd)
# Examples: 300 (5 minutes), 900 (15 minutes), 3600 (1 hour)
FETCH_INTERVAL=3600

# Fetch Since: Time range of IOCs to fetch
# Examples: 15m, 1h, 6h, 24h, 7d
FETCH_SINCE=1h

# Index Configuration
# Index name pattern: {INDEX_NAME}-YYYY.MM.DD
INDEX_NAME=watchtower-iocs
